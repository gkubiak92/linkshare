export const routes = {
  root: "/",
  links: {
    root: "/links",
    tags: "/links/tags",
  },
  login: "/login",
  profile: "/profile",
  register: "/register",
};

export const apiRoutes = {
  logout: "/api/logout",
};
