export const routes = {
  root: "/",
  links: {
    root: "/links",
    tags: "/links/tags",
  },
  login: "/login",
  register: "/register",
};

export const apiRoutes = {
  logout: "/api/logout",
};
